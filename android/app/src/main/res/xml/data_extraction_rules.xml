<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup and transfer policy.

  Every rule is an exclusion. The encrypted database, the attachment files and
  the keystore-backed preferences must never leave this device by any route the
  OS provides: not cloud backup, and not device-to-device transfer during
  setup. A copied database plus a copied key is a copied patient record.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
